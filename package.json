{
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "deploy": "pnpm run gen-events && pnpm run styles-build && zola build && cargo install worker-build ; wrangler deploy",
    "dev": "pnpm run gen-events && pnpm run styles-build && zola --config local.zola.toml build --base-url http://127.0.0.1:1111  && cargo install worker-build ; wrangler dev --local",
    "styles": "pnpm exec tailwindcss --watch -i ./styles/style.css -o ./static/out.css",
    "styles-build": "pnpm exec tailwindcss -i ./styles/style.css -o ./static/out.css",
    "gen-events": "python3 scripts/generate_source_events.py",
    "zsrv": "zola --config local.zola.toml serve --interface 0.0.0.0 --base-url /"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "wrangler": "^4.110.0"
  },
  "dependencies": {
    "powchallenge_client": "^1.0.3"
  }
}
